name: jobs examples

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Step do Job1
        run: echo Hello, world! Job1.
        
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Step do Job2
        run: echo Hello, world! Job2.
